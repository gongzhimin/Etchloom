# 参与贡献

欢迎提交问题、视觉样片和算法改进。这个项目优先保持本地运行、无上传、无构建步骤和可复现生成。

## 开发流程

1. 从 `main` 创建短分支。
2. 使用 `npm start` 检查浏览器工作流。
3. 为生成结果的不变量或已修复的缺陷补充测试。
4. 运行 `npm test`，确认全部测试通过。

问题报告请包含浏览器与操作系统、输入图像尺寸、分析精度、参数、种子和复现步骤。除非图片允许公开，请勿把私人照片提交到仓库；可以使用 `examples/` 中的程序化样片复现问题。

视觉算法改动应同时检查缩略图的大明暗、100% 视图的刻线结构以及最终印样尺寸。实物纸张或油墨结论请附上材料、压力和输出尺寸。
