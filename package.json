{
  "name": "etchloom",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first generative digital printmaking studio.",
  "scripts": {
    "start": "node scripts/serve.cjs",
    "test": "node --test tests/*.test.cjs",
    "benchmark": "node scripts/benchmark.cjs"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
