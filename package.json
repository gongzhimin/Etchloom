{
  "name": "ke-jian-digital-printmaking",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first image-to-engraving and virtual printmaking experiment.",
  "scripts": {
    "start": "node scripts/serve.cjs",
    "test": "node --test tests/*.test.cjs",
    "benchmark": "node scripts/benchmark.cjs"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
